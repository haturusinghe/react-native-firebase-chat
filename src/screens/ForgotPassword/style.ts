import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    marginBottom: 40,
  },
  scrollview: {
    backgroundColor: 'white',
  },
  forgotPassword: {
    fontSize: 14,
    textAlign: 'center',
  },
  about: {
    fontSize: 14,
    textAlign: 'center',
    marginTop: 30,
    bottom: 20,
  },
});
